# Spec-Kit 执行指导：从课件到代码（以第16节为例）

> 本文档已迁移到 [`docs/require/Spec-Kit 执行指导：从课件到代码（以第16节为例）.md`](../require/Spec-Kit%20执行指导：从课件到代码（以第16节为例）.md)。

为避免 Java 旧版指导与当前 Go 课程形成两个事实源，本文件只保留迁移说明。第 16 节当前范围为：Provider 层同时交付 `Generate` 与 `Stream`，ReAct、CLI 和 Web/SSE 流式链路仍属后续特性。
